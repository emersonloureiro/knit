
func main {
    foldersWithAllPermissionsEnabled = "drwx"
    bar = [ls -al | grep '${  foldersWithAllPermissionsEnabled           }'].asList
    bar.foreach {
        line -> {
            print(line)
        }
    }
}