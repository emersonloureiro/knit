
func main {
    assert(1 == 1)
    assert("emerson" == "emerson")
    foo = "foo" == "foo"
    assert(foo)
    bar = "bar"
    assert("bar" == bar)
    assert(knit::str::strContains("foobar", bar))
}